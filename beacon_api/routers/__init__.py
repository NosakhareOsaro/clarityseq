"""
beacon_api.routers
==================
Router package for the GA4GH Beacon v2.1.1 API.

Modules:
    info        — GET /info: Beacon metadata and capabilities.
    g_variants  — GET /g_variants: Genomic variant queries with VRS v2.0 IDs.
    individuals — GET /individuals: Individual-level queries (Passport-gated).
"""
